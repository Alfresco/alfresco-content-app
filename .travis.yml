dist: trusty
sudo: required

services:
  - docker

language: node_js
node_js:
  - "8"

install:
  - npm install -g npm@latest
  - npm ci

script:
  #- npm run test:ci
  - npm run build && npm run e2e:docker
