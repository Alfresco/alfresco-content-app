## Format

The format is represented by a JSON file with the structure similar to the following:

```json
{
  "$name": "app",
  "$version": "1.0.0",

  "routes": [],
  "actions": [],
  "rules": [],
  "features": {}
}
```
